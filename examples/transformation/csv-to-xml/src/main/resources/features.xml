<features>
  <feature name="xml-to-json" version="${project.version}" resolver="(obr)">
    <feature version="${version.camel}">camel-blueprint</feature>
    <feature version="${version.camel}">camel-core</feature>
    <feature version="${version.camel}">camel-dozer</feature>
    <feature version="${version.camel}">camel-xstream</feature>
    <feature version="${version.camel}">camel-bindy</feature>
    <bundle>mvn:org.jboss.tools.fuse.examples.transformation/csv-to-xml/${project.version}</bundle>
  </feature>
</features>
